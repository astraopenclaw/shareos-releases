# ShareOS Releases (OTA)

This repository contains **public** OTA update artifacts for ShareOS.

- `manifest/` — update manifests per channel
- `payloads/` — published payload archives (e.g., OSTree snapshots)

This repo should contain **no private source code**.
